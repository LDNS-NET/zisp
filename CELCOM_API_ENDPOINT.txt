?php

$curl = curl_init();

curl_setopt_array($curl, array(
  CURLOPT_URL => 'https://%7B%7Bdomain%7D%7D/api/services/sendbulk/',
  CURLOPT_RETURNTRANSFER => true,
  CURLOPT_ENCODING => '',
  CURLOPT_MAXREDIRS => 10,
  CURLOPT_TIMEOUT => 0,
  CURLOPT_FOLLOWLOCATION => true,
  CURLOPT_HTTP_VERSION => CURL_HTTP_VERSION_1_1,
  CURLOPT_CUSTOMREQUEST => 'POST',
  CURLOPT_POSTFIELDS =>'{
    "count": 2,
    "smslist": [
        {
            "partnerID": "{{partnerID}}",
            "apikey": "{{apikey}}",
            "pass_type": "plain",
            "clientsmsid": 178234,
            "mobile": "{{mobile}}",
            "message": "This is test message 1",
            "shortcode": "{{shortcode}}"
        },

        {
            "partnerID": "{{partnerID}}",
            "apikey": "{{apikey}}",
            "pass_type": "plain",
            "clientsmsid": 178234,
            "mobile": "{{mobile}}",
            "message": "This is test message 2",
            "shortcode": "{{senderId}}"
        }

    ]
}',
  CURLOPT_HTTPHEADER => array(
    'Content-Type: application/json'
  ),
));

$response = curl_exec($curl);

curl_close($curl);
echo $response;
